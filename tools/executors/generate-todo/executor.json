{
  "executors": {
    "echo": {
      "implementation": "./echo.impl",
      "schema": "./echo.schema.json",
      "description": "Runs `echo` (to test executors out)."
    },
    "generate": {
      "implementation": "./todos.impl",
      "schema": "./todos.schema.json",
      "description": "Generates a bunch of todo's"
    }
  }
}
